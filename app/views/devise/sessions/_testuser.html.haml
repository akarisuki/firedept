%h2 ゲストログイン
= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
    .div
        = f.hidden_field :email ,value: "guest@user"
        = f.hiiden_field :password, value: "adaadaada"
    .actions
        = f.submit "ゲストユーザーでログイン"